package com.example.memory.ui.theme

import androidx.compose.ui.graphics.Color

// Erwachsenes, ruhiges Farbschema (weißer Hintergrund, klare Akzente).
val Ink = Color(0xFF1E293B)            // Haupttext
val Slate = Color(0xFF334155)          // Sekundärtext
val Steel = Color(0xFF475569)          // Tertiärtext
val Paper = Color(0xFFF7F8FA)          // Hintergrund (professionell, leicht gebrochenes Weiß)
val Fog = Color(0xFFF8FAFC)            // Flächen/Karten
val Accent = Color(0xFF2563EB)         // Primär (Buttons, Highlights)
val AccentSoft = Color(0xFFEFF6FF)     // Primär-Container
val Accent2 = Color(0xFF0F766E)        // Sekundär (Statuswerte)
val Accent2Soft = Color(0xFFE6FFFA)    // Sekundär-Container
val Night = Color(0xFF0F172A)          // Hintergrund dunkel
